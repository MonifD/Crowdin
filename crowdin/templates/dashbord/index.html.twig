{% extends 'dashbord.html.twig' %}

{% block title %}Hello DashbordController!{% endblock %}

{% block body %}
    <div class="container my-4">
        <h1 class="text-center mb-4">Dashboard</h1>
        <div class="row mb-3 border rounded p-3 bg-light">
            <div class="col-md-6 border-end">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <h3 class="mb-3">First Name:</h3> 
                        <p class="fs-6">{{ profil.firstName }}</p>
                    </div>
                    <div class="col-md-6">
                        <h3 class="mb-3">Last Name:</h3>  
                        <p class="fs-6">{{ profil.lastName }}</p>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-6">
                        <h3 class="mb-3">Username:</h3> 
                        <p class="fs-6">{{ profil.username }}</p>
                    </div>
                    <div class="col-md-6">
                        <h3 class="mb-3">Email:</h3>  
                        <p class="fs-6">{{ profil.email }}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <h3 class="mb-3">Trad:</h3>  
                        <p class="fs-6">{{ profil.isTrad ? 'Yes' : 'No' }}</p>
                    </div>
                    <div class="col-md-6">
                        <h3 class="mb-3">Created At:</h3> 
                        <p class="fs-6">{{ profil.createdAt ? profil.createdAt|date('Y-m-d H:i:s') : 'N/A' }}</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="my-5"></div>
        <div class="alert alert-info mt-4" role="alert">
            <h4 class="alert-heading">Welcome to Crowding!</h4>
            <p>We're excited to have you on board! Crowding is a user-driven translation platform where you can request translations or become a translator to offer your services. Let's make language learning and communication easier together!</p>
        </div>
        <div class="alert alert-warning mt-4" role="alert">
            <h4 class="alert-heading">Notice: Language and Behavior</h4>
            <p>While using Crowding, please maintain a respectful and courteous demeanor. Any use of offensive language or inappropriate behavior will not be tolerated and may result in account suspension or termination. Let's keep our community welcoming and inclusive for everyone!</p>
        </div>
        <div class="alert alert-secondary mt-4" role="alert">
    <h4 class="alert-heading">Meet the Team!</h4>
    <p>Cesario, Carol, and Mohamad are proud to present their Crowding project in Symfony. We hope you enjoy using our platform!</p>
</div>
    </div>
{% endblock %}
